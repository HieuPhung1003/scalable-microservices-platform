apiVersion: v1
kind: Service
metadata:
  name: user-storage-service
  namespace: microservices-app
spec:
  type: ClusterIP
  selector:
    app: user-storage
  ports:
    - port: 8080
      targetPort: 8080