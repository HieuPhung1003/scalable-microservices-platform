apiVersion: v1
kind: Service
metadata:
  name: redis-storage-service
  namespace: microservices-app
spec:
  type: ClusterIP
  selector:
    app: redis-storage
  ports:
    - port: 8080
      targetPort: 8080